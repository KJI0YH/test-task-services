spring:
  application:
    name: hash-translator
  data:
    mongodb:
      host: mongodb
      port: 27017
      database: application_store

service:
  authorization:
    url: http://authorization:8080/api/login
